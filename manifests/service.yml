apiVersion: v1
kind: Service
metadata:
    name: "democluster"
    labels:
        app: "democluster"
spec:
    type: LoadBalancer
    ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "democluster"